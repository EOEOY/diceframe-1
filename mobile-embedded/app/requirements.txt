# 嵌入运行依赖:与主仓库 requirements.txt 对应,但不带 hash 固定——
# Chaquopy 的 Android wheel 与桌面 wheel 是不同二进制,hash 校验必然失败。
#
# cryptography 下限放宽说明:Chaquopy 预编译仓库(android/py311)最高仅提供
# 42.08;后端只使用 x509/rsa/hashes/serialization 等稳定 API,且 POC 运行于
# 本机回环 HTTP,不做证书签发。桌面端仍保持 >=43,<46 不变,此放宽仅限 APK 内。
--only-binary=:all:
aiohttp>=3.9
cryptography>=42
edge-tts>=7
Pillow>=10
tabulate>=0.10
